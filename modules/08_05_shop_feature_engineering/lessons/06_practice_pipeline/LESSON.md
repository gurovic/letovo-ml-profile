# Lesson Design: Практика: воспроизводимый preprocessing pipeline

## A. Сценарий пары

| Поле | Значение |
|---|---|
| Модуль | Интернет-магазин / маркетплейс: Feature Engineering и lambda (`08_05`) |
| Название урока | Практика: воспроизводимый preprocessing pipeline |
| Пара КТП | **35** |
| Длительность | 2 академических часа (**80 минут**) |
| Роль | интеграция и сдача |
| Пререквизиты | Python и pandas из модулей 1–3; три slim CSV рядом с ноутбуком; схема в [data/README](../../data/README.md) |
| **Открыть** | [lesson.ipynb](lesson.ipynb) — копия на ученика; первая code-ячейка загружает три CSV |
| **Первая фраза** | «Сдаём не таблицу из удачного запуска, а функцию с контрактом, инвариантами и повторяемым результатом» |
| **Минимум сдачи** | функция возвращает 778×7 и лог; негативный тест; повторяемость; acceptance полностью True |
| **Домашнее задание** | [homework.ipynb](homework.ipynb) — Part A обязательно; Challenge после основной части (~1 ч) |
| **Дальше** | следующие модули: временное разбиение и модель |
| **Canvas** | не опубликовано |

### A. Чего хотим от пары

Главное — итоговый pipeline объединяет копирование, контракт, join, признаки, агрегацию и аудит в одной повторяемой функции. Ученик не копирует готовую цепочку: на каждом этапе сначала
называет единицу наблюдения и ожидаемый инвариант, затем пишет код и использует assert
как доказательство. Бизнес-контекст — CRM Olist; метку churn не создаём и модель не обучаем.

Материал ноутбука: §1 Контракт функции, §2 Копии и валидация, §3 Форма результата, §4 Инварианты RFM, §5 Негативный тест, §6 Повторяемость, §7 Preview артефакта, §8 Acceptance и отчёт. CSV лежат рядом, поэтому урок работает офлайн.

---

## B. Ход пары

| # | Этап | ~мин | Ученик | Учитель | Материал | Критерий закрытия |
|---|---|---|---|---|---|---|
| 1 | Контракт функции | 8 | Запишите сигнатуру и docstring, пока тело может быть заглушкой. | «Что должна гарантировать функция при любых входных данных? Сформулируйте до кода.» | `## 1. Контракт функции` | assert раздела зелёный; ученик объясняет проверку |
| 2 | Копии и валидация | 10 | Реализуйте начало функции: копии, schema/key/value checks, первый лог. | «Проверьте: исходный df не изменился после вызова — нет новых столбцов?» | `## 2. Копии и валидация` | assert раздела зелёный; ученик объясняет проверку |
| 3 | Форма результата | 10 | Проверьте одну строку клиента и семь ожидаемых полей. | «assert shape == (778, 7). Какие семь столбцов? Перечислите до запуска.» | `## 3. Форма результата` | assert раздела зелёный; ученик объясняет проверку |
| 4 | Инварианты RFM | 10 | Сверьте сумму заказов, денег и границы. | «Сумма Frequency == 3500 и сумма Monetary == сумма payments — оба инварианта должны пройти.» | `## 4. Инварианты RFM` | assert раздела зелёный; ученик объясняет проверку |
| 5 | Негативный тест | 10 | Подайте отрицательную оплату и проверьте raise. | «Поймайте ValueError через try/except. Проверьте, что лог остался пустым после исключения.» | `## 5. Негативный тест` | assert раздела зелёный; ученик объясняет проверку |
| 6 | Повторяемость | 10 | Запустите дважды и сравните DataFrame и лог. | «Сравните df.sort_values('customer_id') обоих запусков через assert_frame_equal.» | `## 6. Повторяемость` | assert раздела зелёный; ученик объясняет проверку |
| 7 | Preview артефакта | 12 | Сохраните первые 25 строк и прочитайте обратно. | «После to_csv → read_csv: совпадают ли типы столбцов? Что изменилось у дат?» | `## 7. Preview артефакта` | assert раздела зелёный; ученик объясняет проверку |
| 8 | Acceptance и отчёт | 10 | Закройте пять критериев и напишите итог без обещания модели. | «Каждый флаг acceptance вычислен из данных, а не проставлен вручную? Покажите.» | `## 8. Acceptance и отчёт` | assert раздела зелёный; ученик объясняет проверку |

Обязательные этапы для минимума сдачи: **1–7**. §8 закрывает интерпретацию и переход к ДЗ.
Если этап занял больше времени, сокращать письменный объём заметки, но не убирать проверку данных.

---

## C. Если сбились

### Типичные ошибки

| Симптом / мысль ученика | Что сказать или показать |
|---|---|
| Функция меняет исходные DataFrame | Копировать входы в начале и тестировать отсутствие новых столбцов. |
| Результат зависит от порядка строк | Сравнить после сортировки по customer_id. |
| Acceptance заполняется вручную | Каждый флаг вычисляется из результата или лога. |
| Называет features прогнозом | Это transform; задача и модель появятся позже. |

### Дифференциация (кратко)

| | |
|---|---|
| Слабее базы | §§1–4 с парным проговариванием (называем инварианты до реализации); §§5–7 по шаблону с готовой сигнатурой; ДЗ: только Part A §§1–2 |
| Сильнее базы | §§1–8 без подсказок; §8 — все пять acceptance-флагов вычисляются из данных, а не вручную; Challenge: preprocess_customers с параметром ref_date и поддержкой перестановки строк |

---

## D. Проектирование

### Зачем урок

Итоговый pipeline объединяет копирование, контракт, join, признаки, агрегацию и аудит в одной повторяемой функции. Без этой пары следующий шаг превращается в цепочку pandas-команд без контракта:
непонятно, что означает строка, почему сохранилось число объектов и доступен ли признак в момент решения.

### Центральная идея

| Поле | Значение |
|---|---|
| Центральная идея | Итоговый pipeline объединяет копирование, контракт, join, признаки, агрегацию и аудит в одной повторяемой функции. |
| Что поддерживает, но не отвлекает | Реальная схема Olist и CRM-интерпретация; predict и churn остаются вне scope |
| Данные урока | `orders_slim.csv`, `customers_slim.csv`, `payments_slim.csv`; правила RFM — [data/README](../../data/README.md) |

### Результаты обучения

1. Собрать raw→client RFM+ в чистой функции.
2. Сохранить входы неизменными и проверить инварианты.
3. Проверить повторяемость и устойчивость к перестановке строк.
4. Передать preview, quality gate и инженерную записку.

### Профессиональный контекст

Это работа до обучения модели: аналитик превращает источники в воспроизводимую таблицу признаков,
фиксирует ключи, момент доступности, границы и аудит. Такой handoff можно проверять независимо от автора.

### Решения учащегося

| # | Какой выбор делает учащийся | На что влияет |
|---|---|---|
| 1 | Формулирует единицу наблюдения и ожидаемую форму | Корректность join/groupby и отсутствие размножения строк |
| 2 | Выбирает правило преобразования или агрегации | Интерпретируемость и воспроизводимость признака |
| 3 | Решает, какая проверка доказывает результат | Возможность заметить дефект до следующего этапа |

### Материалы (зачем каждый)

- [x] [lesson.ipynb](lesson.ipynb) — §§1–8, stubs и исполняемые assert
- [x] [homework.ipynb](homework.ipynb) — Part A + Challenge
- [x] [solutions.ipynb](solutions.ipynb) — секционные эталоны урока и ДЗ, только преподавателю
- [x] три slim CSV — автономный запуск без Kaggle
- [ ] Canvas — не опубликовано
- [ ] презентация — не нужна: ход и фиксации встроены в ноутбук

### Домашнее задание

| Поле | Значение |
|---|---|
| Назначается | **да** |
| Файл | [homework.ipynb](homework.ipynb) |
| Формулировка | Три задания Part A закрепляют вычисление и gate; Challenge переносит идею в функцию и письменное решение |
| Ориентир времени | **~1 ч** |
| Почему не на уроке | Самостоятельная функция и обоснование требуют повторного запуска без пошаговых подсказок |
| Какую способность развивает | Перенос контракта признака в новый, но структурно похожий сценарий |

---

## E. Карточка урока (§13)

| Поле | Значение |
|---|---|
| Часы | 2 |
| Стратегии обучения / виды деятельности | Guided coding → самостоятельная секция; прогноз формы до запуска; чтение assert как контракта |
| Формирующее оценивание | Зелёные assert §§1–7; объяснение одного инварианта и одного ограничения |
| Дифференциация (общая) | База: §§1–4 с парным формулированием инвариантов; усиление: §§5–8 самостоятельно + Challenge preprocess_customers с параметром ref_date |
| По содержанию | Итоговый pipeline объединяет копирование, контракт, join, признаки, агрегацию и аудит в одной повторяемой функции. |
| По процессу | Индивидуальный код, короткие сверки после §§2, 5 и 7, взаимная проверка формулировки §8 |
| По продукту | Заполненный `lesson.ipynb`; сданный `homework.ipynb` Part A; Challenge по готовности |
| Canvas | не опубликовано |
