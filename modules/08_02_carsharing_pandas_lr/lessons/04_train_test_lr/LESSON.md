# Lesson Design: train/test split; LinearRegression fit/predict

## A. Сценарий пары

| Поле | Значение |
|---|---|
| Модуль | pandas и линейная регрессия (`08_02`) |
| Название урока | train/test split; LinearRegression fit/predict |
| Пара КТП | **14** |
| Длительность | 2 академических часа (**80 минут**) |
| Роль | введение |
| Пререквизиты | Пара 13: EDA; модуль 1: идея predict |
| **Открыть** | [lesson.ipynb](lesson.ipynb) + `trips.csv` рядом (как на паре 11). Импорты sklearn уже в первой code-ячейке |
| **Первая фраза** | «Ручной predict из модуля 1 теперь делает библиотека: разделим данные, обучим линейную модель на расстоянии» |
| **Минимум сдачи** | (1) X и y выделены; (2) split 80/20, `random_state=42`; (3) fit на train, predict на test; (4) `coef_` и `intercept_` напечатаны. `## 4. Расширение` — по времени, не в минимум |
| **Домашнее задание** | [homework.ipynb](homework.ipynb) — сдать заполненный ноутбук (~1 ч) |
| **Дальше** | метрики — [пара 15](../05_practice_metrics/LESSON.md) |
| **Canvas** | Модуль 54689: план скрыт; [урок Colab](https://colab.research.google.com/gist/gurovic/4319bd3960b3b50ebc04ea49b3623ff3/lesson.ipynb); ДЗ — Assignment ([Colab](https://colab.research.google.com/gist/gurovic/4319bd3960b3b50ebc04ea49b3623ff3/homework.ipynb)) |

### Ориентир времени (не поминутный)

| Блок | ~мин |
|---|---|
| X и y | 15 |
| train/test | 25 |
| fit / predict | 30 |
| Постановка ДЗ | 10 |
| **Итого** | **80** |

---

## B. Ход пары

| # | Этап | ~мин | Ученик | Учитель | Материал | Критерий закрытия |
|---|---|---|---|---|---|---|
| 1 | Признак и цель | 15 | X = [[distance]], y = duration | Двойные скобки у X | `## 1. Признак и цель` | shape X и y |
| 2 | Split | 25 | train_test_split, seed 42 | Зачем test отдельно | `## 2. train/test split` | assert длины test |
| 3 | fit/predict | 30 | LinearRegression; предсказания test | «`coef_` — тот же смысл, что k в ручном predict модуля 1» | `## 3. fit / predict` | `coef_` / `intercept_` напечатаны |
| 3б | (если есть время) | — | Split с `random_state=0` — совпали ли индексы test? | Seed меняет состав | `## 4. Расширение` | bool-ответ |
| 4 | Постановка ДЗ | 10 | Другой test_size | Воспроизводимость seed | [homework.ipynb](homework.ipynb) | знает срок |

---

## C. Если сбились

### Типичные ошибки

| Симптом / мысль ученика | Что сказать или показать |
|---|---|
| X одномерный Series | `df[[col]]` — DataFrame из одного столбца |
| fit на всех данных | Сначала split |
| Метрики на этой паре | MSE/R² — следующая пара |

### Дифференциация (кратко)

| | |
|---|---|
| Слабее базы | Этапы 1–3 с фиксированным `test_size=0.2`; coef/intercept — распечатать из готового `model`; в ДЗ — задача 1 |
| Сильнее базы | Устно: `coef_` ≈ коэффициент k из ручного `predict` модуля 1; в ДЗ — воспроизводимость seed и вызов обязательны |

---

## D. Проектирование

### Зачем урок

Связка модуля 1 (`predict` как функция) с sklearn: без train/test и `fit` регрессия остаётся ручным скриптом. Метрики сознательно отложены на пару 15.

### Центральная идея

| Поле | Значение |
|---|---|
| Центральная идея | Обучение = `fit` на train; проверка обобщения — на отложенном test (метрики — пара 15) |
| Что поддерживает, но не отвлекает | MSE/R² — пара 15; другой `test_size` и повтор seed — в ДЗ |
| Данные урока | `data/trips.csv`; признак `distance_km`, цель `duration_min` |

### Результаты обучения

1. Выделить `X` (DataFrame одного столбца) и `y`.
2. Разделить данные `train_test_split` с `random_state=42`.
3. Обучить `LinearRegression`, получить `predict` на test и напечатать `coef_` / `intercept_`.

### Профессиональный контекст

В проде модель учат на известных поездках и проверяют на «новых» — train/test имитирует эту границу до появления метрик.

### Решения учащегося

| # | Какой выбор делает учащийся | На что влияет |
|---|---|---|
| 1 | Форма `X`: `df[[col]]` vs Series | Совместимость с sklearn API |
| 2 | Фиксировать `random_state` | Воспроизводимость эксперимента и честное сравнение позже |

### Материалы (зачем каждый)

- [x] ноутбук `lesson.ipynb` — X/y, split, fit/predict
- [x] [homework.ipynb](homework.ipynb) — другой `test_size`, проверка seed, вызов
- [x] [solutions.ipynb](solutions.ipynb) — эталон (только преподаватель)
- [ ] презентация — не нужна

### Домашнее задание

| Поле | Значение |
|---|---|
| Назначается | **да** |
| Файл | [homework.ipynb](homework.ipynb) |
| Формулировка | Split с другим `test_size`; два прогона с одним seed; вызов — что меняется при смене seed |
| Ориентир времени | **~1 ч** (0,5–1,5 ч) |
| Почему не на уроке | На паре — один протокол 80/20; эксперименты с долей и seed требуют отдельного времени |
| Какую способность развивает | Воспроизводимость; понимание, что split влияет на состав train/test |

---

## E. Карточка урока (§13)

| Поле | Значение |
|---|---|
| Часы | 2 |
| Стратегии обучения / виды деятельности | Guided coding: split → fit → predict на одном признаке |
| Формирующее оценивание | Рабочий split и `model.predict`; coef/intercept выведены |
| Дифференциация | Слабее — фиксированный 80/20; сильнее — связь с модулем 1 и ДЗ про seed |
| По содержанию | Один признак и один split на паре; вариации test_size/seed — в ДЗ |
| По процессу | Код индивидуально; разбор формы X — у доски |
| По продукту | Обученная модель и pred на test; сдача `homework.ipynb` |
| Canvas | Модуль 54689: план скрыт; урок Colab; ДЗ Assignment |
