# Lesson Design: pandas: read_csv, DataFrame, feature / target

## A. Сценарий пары

| Поле | Значение |
|---|---|
| Модуль | pandas и линейная регрессия (`08_02`) |
| Название урока | pandas: DataFrame, feature/target; фильтры и срезы |
| Пара КТП | **9** |
| Длительность | 2 академических часа (**80 минут**) |
| Роль | введение + отработка фильтров (две бывшие пары в одной) |
| Пререквизиты | Модуль 1: функция, return, predict; Python 7 класса |
| **Открыть** | Сначала [lesson.ipynb](lesson.ipynb); во второй половине — [filters/lesson.ipynb](filters/lesson.ipynb). CSV рядом |
| **Первая фраза** | «Сначала схема таблицы (цель / признаки / id), затем выбираем объявления фильтрами» |
| **Минимум сдачи** | Схема: `TARGET`/`FEATURES`/`ID_COL` + assert. Фильтры: center, accommodates≥6, loc/iloc, `number_of_reviews` как int. Детали фильтров — в [filters/LESSON.md](filters/LESSON.md) (не переписывать тему) |
| **Домашнее задание** | [homework.ipynb](homework.ipynb) и/или [filters/homework.ipynb](filters/homework.ipynb) — по указанию учителя (~1 ч суммарно) |
| **Дальше** | [пара 10](../02_eda_scatter/LESSON.md) |
| **Canvas** | Модуль 54689: план скрыт; [урок Colab](https://colab.research.google.com/gist/gurovic/d19afee5ef0dcd2be593ea93d11ba26f/lesson.ipynb); ДЗ — Assignment ([Colab](https://colab.research.google.com/gist/gurovic/d19afee5ef0dcd2be593ea93d11ba26f/homework.ipynb)) |

### A. Чего хотим от пары

Главное — перейти от списков модуля 1 к таблице: загрузить CSV, назвать роли столбцов (target / features / id), затем **на той же паре** закрепить фильтры и срезы (бывшая отдельная пара 10 — материалы перенесены в [filters/](filters/), тему не переписывали).

Побочно: `shape`/`dtypes`/`value_counts`. Модель на этой паре не вводим.

### Ориентир времени (не поминутный)

| Блок | ~мин |
|---|---|
| Схема DataFrame (ноутбук `lesson.ipynb`) | 35 |
| Фильтры / loc / типы ([filters/lesson.ipynb](filters/lesson.ipynb)) | 35 |
| Постановка ДЗ + буфер | 10 |
| **Итого** | **80** |

---

## B. Ход пары

| # | Этап | ~мин | Ученик | Учитель | Материал | Критерий закрытия |
|---|---|---|---|---|---|---|
| 1 | Зачем таблица | 10 | shape, head, columns | «Строка = объявление» | `## 1. Зачем таблица` | shape на экране |
| 2 | dtypes | 10 | Сравнивает dtype accommodates vs neighbourhood | Число vs object | `## 2. dtypes: число vs текст` | устно: почему accommodates не object |
| 3 | Ячейка / loc–iloc | 15 | iloc + срез loc/iloc | Позиция vs имя | `## 3`–`## 4` | срезы совпали |
| 4 | Роли столбцов | 15 | TARGET / FEATURES / ID_COL | Не путать id с признаком | `## 5. Признак, цель, служебный столбец` | assert зелёные |
| 5 | value_counts + checkpoint | 15 | neighbourhood counts; `WHY_DATAFRAME` | Почему не list of dicts | `## 6`–`## 7` | непустой WHY |
| 5б | (если есть время) | — | Объявление L0001 | id не в X | `## 8. Расширение: объявление по id` | price напечатан |
| 6 | Постановка ДЗ | 15 | Открывает homework.ipynb | Срез и entire | [homework.ipynb](homework.ipynb) | знает файл и срок |

---

## C. Если сбились

### Типичные ошибки

| Симптом / мысль ученика | Что сказать или показать |
|---|---|
| `df` не определён | Сначала ячейка с `read_csv` |
| Кладут listing_id в FEATURES | Id не подаём в модель |
| Путают строку и столбец | Показать `df['accommodates']` vs `df.iloc[0]` |

### Дифференциация (кратко)

| | |
|---|---|
| Слабее базы | Этапы 1–2: shape/head + TARGET/FEATURES/ID_COL с assert; этап 3 — только назвать object-столбцы устно, без связи с fit |
| Сильнее базы | Этап 3 полностью; в ДЗ — задача 4 (`mean` entire vs всей таблицы) обязательна |

---

## D. Проектирование

### Зачем урок

Первый вход в табличные данные: без явных ролей столбцов (цель / признак / id) дальнейший `fit` превращается в угадывание имён. Переход от списков модуля 1 к DataFrame.

### Центральная идея

| Поле | Значение |
|---|---|
| Центральная идея | Таблица объявлений — DataFrame: цель, признаки и id различают **до** любой модели |
| Что поддерживает, но не отвлекает | Фильтры — вторая половина этой же пары ([filters/](filters/)); модель — с пары 11 |
| Данные урока | `data/listings.csv` (краткосрочная аренда «StayLocal»); копия рядом с ноутбуком при необходимости |

### Результаты обучения

1. Загрузить CSV в DataFrame и показать `shape` / `head`.
2. Заполнить `TARGET`, `FEATURES`, `ID_COL` и пройти assert.
3. Назвать столбцы типа `object` и объяснить, почему `accommodates` числовой.

### Профессиональный контекст

Аналитик краткосрочной аренды получает CSV объявлений: прежде чем прогнозировать цену за ночь, нужно зафиксировать схему — что предсказываем, чем объясняем, что служебное.

### Решения учащегося

| # | Какой выбор делает учащийся | На что влияет |
|---|---|---|
| 1 | Какие столбцы в `FEATURES`, что в `ID_COL` | Контракт будущей модели: id не попадает в X |
| 2 | Считать ли `neighbourhood` / `room_type` признаками на этом этапе | Пока только числовые признаки в списке; категориальные — вне fit модуля |

### Материалы (зачем каждый)

- [x] ноутбук `lesson.ipynb` — ход пары (§1–3)
- [x] [homework.ipynb](homework.ipynb) — срез столбцов, поиск по `listing_id`, два способа длины, вызов entire
- [x] [solutions.ipynb](solutions.ipynb) — эталон (только преподаватель)
- [ ] презентация — не нужна

### Домашнее задание

| Поле | Значение |
|---|---|
| Назначается | **да** |
| Файл | [homework.ipynb](homework.ipynb) |
| Формулировка | Срез столбцов; цена `L0001`; `len` vs `shape[0]`; среднее `price` для `entire` vs всей таблицы |
| Ориентир времени | **~1 ч** (0,5–1,5 ч) |
| Почему не на уроке | На паре — схема и типы; поиск по id и сравнение групп требуют спокойной работы с индексацией |
| Какую способность развивает | Чтение таблицы по ключу; сверка длины; простая агрегация по фильтру |

---

## E. Карточка урока (§13)

| Поле | Значение |
|---|---|
| Часы | 2 |
| Стратегии обучения / виды деятельности | Осмотр DataFrame; разделение столбцов на цель / признаки / id; разбор dtype |
| Формирующее оценивание | assert на TARGET/FEATURES/ID_COL; устно: роль столбцов и object |
| Дифференциация | Слабее — схема столбцов + устный object; сильнее — полный этап 3 и ДЗ-вызов entire |
| По содержанию | Схема DataFrame vs поиск/агрегация в ДЗ |
| По процессу | Код индивидуально; разбор id vs feature — у доски |
| По продукту | Рабочие `TARGET`/`FEATURES`/`ID_COL`; сдача `homework.ipynb` |
| Canvas | Модуль 54689: план скрыт; урок Colab; ДЗ Assignment |
