# Lesson Design: pandas: read_csv, DataFrame, feature / target

## A. Сценарий пары

| Поле | Значение |
|---|---|
| Модуль | pandas и линейная регрессия (`08_02`) |
| Название урока | pandas: read_csv, DataFrame, feature / target |
| Пара КТП | **11** |
| Длительность | 2 академических часа (**80 минут**) |
| Роль | введение |
| Пререквизиты | Модуль 1: функция, return, predict; Python 7 класса |
| **Открыть** | [lesson.ipynb](lesson.ipynb) + `trips.csv` рядом с ноутбуком (уже в папке урока). Первая code-ячейка сама ищет CSV (`trips.csv` / `../../data/trips.csv`) |
| **Первая фраза** | «Сегодня списки превращаются в таблицу: загрузим поездки каршеринга и назовём столбцы — цель, признаки, id» |
| **Минимум сдачи** | (1) `df.shape` и `head`; (2) dtypes distance vs zone; (3) `TARGET` / `FEATURES` / `ID_COL`; (4) `WHY_DATAFRAME`. Блок `## 8. Расширение` — по времени |
| **Домашнее задание** | [homework.ipynb](homework.ipynb) — сдать заполненный ноутбук (~1 ч) |
| **Дальше** | фильтры — [пара 12](../02_practice_filters/LESSON.md) |
| **Canvas** | Модуль 54689: план скрыт; [урок Colab](https://colab.research.google.com/gist/gurovic/d19afee5ef0dcd2be593ea93d11ba26f/lesson.ipynb); ДЗ — Assignment ([Colab](https://colab.research.google.com/gist/gurovic/d19afee5ef0dcd2be593ea93d11ba26f/homework.ipynb)) |

### Ориентир времени (не поминутный)

| Блок | ~мин |
|---|---|
| Загрузка и осмотр | 20 |
| Feature / target / id | 25 |
| Типы object vs число | 20 |
| Постановка ДЗ + буфер | 15 |
| **Итого** | **80** |

---

## B. Ход пары

| # | Этап | ~мин | Ученик | Учитель | Материал | Критерий закрытия |
|---|---|---|---|---|---|---|
| 1 | Зачем таблица | 10 | shape, head, columns | «Строка = поездка» | `## 1. Зачем таблица` | shape на экране |
| 2 | dtypes | 10 | Сравнивает dtype distance vs zone | Число vs object | `## 2. dtypes: число vs текст` | устно: почему distance не object |
| 3 | Ячейка / loc–iloc | 15 | iloc + срез loc/iloc | Позиция vs имя | `## 3`–`## 4` | срезы совпали |
| 4 | Роли столбцов | 15 | TARGET / FEATURES / ID_COL | Не путать id с признаком | `## 5. Признак, цель, служебный столбец` | assert зелёные |
| 5 | value_counts + checkpoint | 15 | zone counts; `WHY_DATAFRAME` | Почему не list of dicts | `## 6`–`## 7` | непустой WHY |
| 5б | (если есть время) | — | Поездка T0001 | id не в X | `## 8. Расширение: поездка по id` | duration напечатан |
| 6 | Постановка ДЗ | 15 | Открывает homework.ipynb | Срез и comfort | [homework.ipynb](homework.ipynb) | знает файл и срок |

---

## C. Если сбились

### Типичные ошибки

| Симптом / мысль ученика | Что сказать или показать |
|---|---|
| `df` не определён | Сначала ячейка с `read_csv` |
| Кладут trip_id в FEATURES | Id не подаём в модель |
| Путают строку и столбец | Показать `df['distance_km']` vs `df.iloc[0]` |

### Дифференциация (кратко)

| | |
|---|---|
| Слабее базы | Этапы 1–2: shape/head + TARGET/FEATURES/ID_COL с assert; этап 3 — только назвать object-столбцы устно, без связи с fit |
| Сильнее базы | Этап 3 полностью; в ДЗ — задача 4 (`mean` comfort vs всей таблицы) обязательна |

---

## D. Проектирование

### Зачем урок

Первый вход в табличные данные: без явных ролей столбцов (цель / признак / id) дальнейший `fit` превращается в угадывание имён. Переход от списков модуля 1 к DataFrame.

### Центральная идея

| Поле | Значение |
|---|---|
| Центральная идея | Таблица поездок — DataFrame: цель, признаки и id различают **до** любой модели |
| Что поддерживает, но не отвлекает | Фильтры и срезы — пара 12; модель — с пары 14 |
| Данные урока | `data/trips.csv` (каршеринг «Дорога»); копия рядом с ноутбуком при необходимости |

### Результаты обучения

1. Загрузить CSV в DataFrame и показать `shape` / `head`.
2. Заполнить `TARGET`, `FEATURES`, `ID_COL` и пройти assert.
3. Назвать столбцы типа `object` и объяснить, почему `distance_km` числовой.

### Профессиональный контекст

Аналитик каршеринга получает CSV поездок: прежде чем прогнозировать длительность, нужно зафиксировать схему — что предсказываем, чем объясняем, что служебное.

### Решения учащегося

| # | Какой выбор делает учащийся | На что влияет |
|---|---|---|
| 1 | Какие столбцы в `FEATURES`, что в `ID_COL` | Контракт будущей модели: id не попадает в X |
| 2 | Считать ли `zone` / `vehicle_type` признаками на этом этапе | Пока только числовые признаки в списке; категориальные — вне fit модуля |

### Материалы (зачем каждый)

- [x] ноутбук `lesson.ipynb` — ход пары (§1–3)
- [x] [homework.ipynb](homework.ipynb) — срез столбцов, поиск по `trip_id`, два способа длины, вызов comfort
- [x] [solutions.ipynb](solutions.ipynb) — эталон (только преподаватель)
- [ ] презентация — не нужна

### Домашнее задание

| Поле | Значение |
|---|---|
| Назначается | **да** |
| Файл | [homework.ipynb](homework.ipynb) |
| Формулировка | Срез столбцов; длительность `T0001`; `len` vs `shape[0]`; среднее `duration_min` для `comfort` vs всей таблицы |
| Ориентир времени | **~1 ч** (0,5–1,5 ч) |
| Почему не на уроке | На паре — схема и типы; поиск по id и сравнение групп требуют спокойной работы с индексацией |
| Какую способность развивает | Чтение таблицы по ключу; сверка длины; простая агрегация по фильтру |

---

## E. Карточка урока (§13)

| Поле | Значение |
|---|---|
| Часы | 2 |
| Стратегии обучения / виды деятельности | Осмотр DataFrame; разделение столбцов на цель / признаки / id; разбор dtype |
| Формирующее оценивание | assert на TARGET/FEATURES/ID_COL; устно: роль столбцов и object |
| Дифференциация | Слабее — схема столбцов + устный object; сильнее — полный этап 3 и ДЗ-вызов comfort |
| По содержанию | Схема DataFrame vs поиск/агрегация в ДЗ |
| По процессу | Код индивидуально; разбор id vs feature — у доски |
| По продукту | Рабочие `TARGET`/`FEATURES`/`ID_COL`; сдача `homework.ipynb` |
| Canvas | Модуль 54689: план скрыт; урок Colab; ДЗ Assignment |
