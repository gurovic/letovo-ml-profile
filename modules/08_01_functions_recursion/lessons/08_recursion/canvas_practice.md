# Практика из Canvas (ML-адаптация)

Задачи из контеста «Рекурсия» (IT-8), отобранные под ML.

---

## F. Max digit в feature_id

Строка `"apt_128_floor_3"` — рекурсивно найти **максимальную цифру** (без `max()` по строке).

---

## Fibonacci — антипример

1. Реализовать наивный `fib(n)`.
2. Сравнить с итерацивной версией для n=25.
3. Вывод: для pipeline — итерация; рекурсия — для **структур данных**.

---

## Не включено

Hanoi, reverse без списка, вставка `*`, скобки, +3/+5.
