# Стартовый код `text_stats`

Реализуйте функции в `text_stats.py` по [PROJECT.md](../PROJECT.md).

## Запуск тестов

```bash
cd modules/08_01_functions_recursion/artifact/starter
python manual_tests.py
```

Пока функции не реализованы, тесты падают с `NotImplementedError` — это нормально.

## Что сдать

1. `text_stats.py` — все функции из PROJECT.md.
2. `README.md` (создать) — pipeline, 5 тестовых фраз для `naive_classify`, связь с NLP (1 абзац).
3. Прохождение `manual_tests.py` (10 assert).

Эталон для самопроверки учителя: [../solution/](../solution/) (не копировать в работу).
